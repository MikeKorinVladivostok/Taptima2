{% extends 'base.html.twig' %}


{% block body %}
    {% for post in authors %}
        <p>{{ post.id }}</p>
        <p>{{ post.book}}</p>
        <p>{{ post.author }}</p>
    {% endfor %}
{% endblock %}
